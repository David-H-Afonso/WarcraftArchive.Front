// Runtime environment config – replaced by Docker entrypoint
// In development: leave empty to use Vite proxy / hardcoded dev URL
window.ENV = { VITE_API_BASE_URL: 'WARCRAFT_API_URL_PLACEHOLDER' }
